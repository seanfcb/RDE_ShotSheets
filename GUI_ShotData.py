import subprocess

# Run ShotMatrix_GUI.py and wait for it to complete
subprocess.run(['python3', 'ShotMatrix_GUI.py'])

# Run ScopeChannels_GUI.py and wait for it to complete
subprocess.run(['python3', 'ScopeChannels_GUI.py'])

# Run HSCameraSet_GUI.py and wait for it to complete
subprocess.run(['python3', 'HSCameraSet_GUI.py'])
